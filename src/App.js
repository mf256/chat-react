import React from 'react';
import { BrowserRouter as Router, Route } from 'react-router-dom';
import './App.css';

const App = () => {
  <Router>
    <Routre path="/" exact component={Join} />
    <Routre path="/chat" component={Chat} />
  </Router>
}

export default App;
